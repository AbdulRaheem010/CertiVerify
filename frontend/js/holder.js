import{api}from'./api.js';import{escapeHtml,formatDate,statusBadge}from'./utils.js';document.addEventListener('DOMContentLoaded',async()=>{const root=document.querySelector('#holder-certificates');try{const {items}=await api.get('/holder/certificates');root.innerHTML=items.length?items.map(c=>`<article class="template-card"><div class="template-art"><span>${escapeHtml(c.organization.name)}</span><b>${escapeHtml(c.title)}</b><small>${escapeHtml(c.course.name)}</small></div><div class="template-card-body"><div><h2>${escapeHtml(c.course.name)}</h2><p>${formatDate(c.issueDate)} · ${statusBadge(c.status)}</p></div></div><div class="card-actions"><a href="/holder-certificate.html?id=${c.id}">View credential</a><a href="${c.certificateFileUrl||'#'}">Download</a></div></article>`).join(''):'<div class="empty-state"><h2>No credentials yet</h2><p>Your verified certificates will appear here.</p></div>';}catch(e){root.innerHTML=`<div class="empty-state">${escapeHtml(e.message)}</div>`;}});
