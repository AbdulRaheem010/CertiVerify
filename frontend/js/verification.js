import {api} from './api.js';import{escapeHtml,statusBadge,formatDate}from'./utils.js';
async function verify(id){const result=document.querySelector('#verification-result');result.innerHTML='<div class="skeleton certificate-skeleton"></div>';result.hidden=false;try{const c=await api.get(`/verify/${encodeURIComponent(id)}`);document.title=`${c.title} — Verified by CertiVerify`;result.innerHTML=`<div class="verification-banner ${c.status.toLowerCase()}"><strong>${c.status==='VALID'?'✓ VERIFIED CREDENTIAL':`CREDENTIAL ${c.status}`}</strong><span>Verified on CertiVerify</span></div><article class="certificate-result"><div class="org-mark">${escapeHtml(c.organizationName.slice(0,2))}</div><p class="eyebrow">${escapeHtml(c.organizationName)}</p><h1>${escapeHtml(c.title)}</h1><p class="completion">This credential is awarded to</p><h2>${escapeHtml(c.recipientName)}</h2><p class="completion">for successfully completing</p><h3>${escapeHtml(c.courseName)}</h3><div class="certificate-meta"><div><small>Certificate ID</small><b>${escapeHtml(c.certificateId)}</b></div><div><small>Issue date</small><b>${formatDate(c.issueDate)}</b></div><div><small>Status</small>${statusBadge(c.status)}</div>${c.expiryDate?`<div><small>Expiry date</small><b>${formatDate(c.expiryDate)}</b></div>`:''}</div>${c.status==='REVOKED'?`<p class="revocation">Revoked ${formatDate(c.revokedAt)}. ${escapeHtml(c.revokedReason||'')}</p>`:''}<div class="verify-actions"><button class="button secondary" onclick="navigator.clipboard.writeText(location.href)">Copy verification link</button><a class="button" href="mailto:?subject=Verified credential&body=${encodeURIComponent(location.href)}">Share credential</a></div></article>`;}catch(error){result.innerHTML=`<div class="not-found"><div class="not-found-icon">!</div><h2>Credential not found</h2><p>${escapeHtml(error.message)}</p><p>Check the certificate ID and try again.</p></div>`}}
document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('#verify-form'), input=document.querySelector('#certificate-id'),id=new URLSearchParams(location.search).get('id');form?.addEventListener('submit',e=>{e.preventDefault();const value=input.value.trim().toUpperCase();history.replaceState({},'',`?id=${encodeURIComponent(value)}`);verify(value)});if(id){input.value=id;verify(id)}});
